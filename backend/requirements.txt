# ── Web framework ───────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# ── Database (Firestore) ────────────────────────────────────────────
firebase-admin==6.6.0
# Pinned: google-cloud-firestore >=2.20 requires protobuf >=6, which breaks
# google-generativeai (needs <6). Do not bump without checking both.
google-cloud-firestore==2.19.0

# ── Cache / short-term memory ───────────────────────────────────────
redis==5.2.1

# ── LLM + agents (Google Gemini) ────────────────────────────────────
google-generativeai==0.8.3
langchain==0.3.13
langchain-core==0.3.28
langchain-community==0.3.13
langchain-google-genai==2.0.7
langgraph==0.2.60

# ── RAG / vector store ──────────────────────────────────────────────
qdrant-client==1.12.1
langchain-qdrant==0.2.0
pymupdf==1.25.1

# Shared transitive pins. qdrant-client pulls the newest grpcio-tools, which
# drags in protobuf 7 and silently breaks the Gemini SDK at runtime. These two
# lines are what keep Firestore, Gemini, and Qdrant installable together.
grpcio-tools==1.68.1
protobuf>=5.26.1,<6

# ── Research tools ──────────────────────────────────────────────────
tavily-python==0.5.0
playwright==1.49.1
requests==2.32.3
httpx==0.28.1

# ── Comms ───────────────────────────────────────────────────────────
# Email goes out over SMTP (stdlib smtplib); Resend is an optional
# alternative sender. Twilio is present but WhatsApp is switched off.
resend==2.5.1
twilio==9.4.1
APScheduler==3.11.0

# ── Utilities ───────────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==9.0.0
