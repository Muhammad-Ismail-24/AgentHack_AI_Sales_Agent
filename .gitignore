# Dependencies
node_modules/
.venv/
__pycache__/
*.pyc
*.pyo

# Build outputs
dist/
build/
.next/
*.egg-info/

# Secrets
.env
.env.*
*.pem
*.key

# Firebase / GCP service account credentials — never commit these.
# See conflicts.md for the comms-local Firestore adapter that reads one
# via the FIREBASE_SERVICE_ACCOUNT_PATH env var (path never hardcoded).
*firebase-adminsdk*.json
serviceAccountKey.json
*serviceAccount*.json

# Data
data/uploads/
data/cache/

# Local settings
.claude/settings.local.json

# Logs and temp files
*.log
*.tmp
.DS_Store